<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY — permits cleartext HTTP to any host.
  This file lives in src/debug/ so it only applies to debug builds.
  The release build uses src/main/res/xml/network_security_config.xml
  which keeps HTTPS enforcement intact.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
